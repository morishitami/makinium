<h2>新規登録</h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field">
    <div class='form-text-wrap'>
      <label class="form-text">会員番号</label>
      <span class="indispensable">必須</span>
    </div>
    <%= f.text_field :code, class:"input-default", maxlength:"4", placeholder:"半角数字", autofocus: true %>
  </div>

  <div class="field">
    <div class='form-text-wrap'>
      <label class="form-text">本名</label>
      <span class="indispensable">必須</span>
    </div>
    <%= f.text_field :name, class:"input-default", placeholder:"例) 鈴木太郎" %>
  </div>

  <div class="field">
    <div class='form-text-wrap'>
      <label class="form-text">芸名</label>
      <span class="form-any">任意</span>
    </div>
    <%= f.text_field :stage_name, class:"input-default", placeholder:"本名と異なる場合は入力してください" %>
  </div>

  <div class="field">
    <div class='form-text-wrap'>
    <label class="form-text">パスワード</label>
      <span class="indispensable">必須</span>
    </div>
    <%= f.password_field :password, class:"input-default", placeholder:"6文字以上の半角英数字" %>
    <p class='info-text'>※英字と数字の両方を含めて設定してください</p>
  </div>

  <div class="field">
    <div class='form-text-wrap'>
    <label class="form-text">パスワード再確認</label>
      <span class="indispensable">必須</span>
    </div>
    <%= f.password_field :password_confirmation, class:"input-default", placeholder:"同じパスワードを入力して下さい" %>
  </div>

  <div class="actions">
    <%= f.submit "新規登録する" %>
  </div>
<% end %>

<%#= render "devise/shared/links" %>
